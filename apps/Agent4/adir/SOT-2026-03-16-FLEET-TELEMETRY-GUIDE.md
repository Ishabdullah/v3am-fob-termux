**324 Ports and paths are changed ref data**

# FLEET TELEMETRY GUIDE 
## WINDOWS DISCOVERY 
2. Processes: `tasklist /V /FI "MEMUSAGE gt 100000"` 
## GPU TELEMETRY 
Standard Command: `nvidia-smi` 
Usage: Check VRAM for Ollama offloading. 
## WSL / DOCKER BRIDGE 
1. List Distros: `wsl -l -v` 
## USER PATH VERIFICATION 
Always run `dir C:\Users` before assuming a username.

**324 Ports and paths are changed ref data**
